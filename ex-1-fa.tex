\documentclass[tikz]{standalone}
\usepackage{tikz}
\usetikzlibrary{automata, positioning}

\begin{document}\begin{tikzpicture}[shorten >=1pt, node distance=2cm, on grid, auto]

  % Nodes in a horizontal line
  \node[state, initial, accepting] (q0) {0};
  \node[state, right of=q0] (q1) {1};
  \node[state, right of=q1] (q2) {2};
  \node[state, right of=q2] (q3) {3};
  \node[state, right of=q3] (q4) {4};
  \node[state, right of=q4] (q5) {5};
  \node[state, right of=q5] (q6) {6};
  \node[state, right of=q6] (q7) {7};
  \node[state, right of=q7] (q8) {8};
  \node[state, right of=q8] (q9) {9};

  % "inc" transitions from 0 to 9
  \path[->] (q0) edge node {0,1} (q1);
  \path[->] (q1) edge node {0,1} (q2);
  \path[->] (q2) edge node {0,1} (q3);
  \path[->] (q3) edge node {0,1} (q4);
  \path[->] (q4) edge node {0,1} (q5);
  \path[->] (q5) edge node {0,1} (q6);
  \path[->] (q6) edge node {0,1} (q7);
  \path[->] (q7) edge node {0,1} (q8);
  \path[->] (q8) edge node {0,1} (q9);
  \path[->, bend right=40] (q9) edge node[above] {0,1} (q0);

\end{tikzpicture}
\end{document}
